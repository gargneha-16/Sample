public with sharing class PromotionExecutionService extends ResultStatusHelper implements ResultStatusLWCInterface {
    // Will be deleted

    public ResultStatusDetail execute(Id contextId) {
        return null; 
    }
}